resources:
  pipelines:
    games_social_listening_pipeline:
      name: ${var.prefix} - Games Social Listening Pipeline
      libraries:
        - glob:
            include: ../src/pipeline/transformations/**
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: ../src/pipeline/
